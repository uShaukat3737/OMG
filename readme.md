# Project OMG - Cloud System App (Flask)

This is a simple demo cloud application built with **Python Flask**.

## 🚀 How to Run
1. Create a virtual environment (recommended):
   ```bash
   python3 -m venv venv
   source venv/bin/activates